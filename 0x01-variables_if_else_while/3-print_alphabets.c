#include <time.h>


/**
 * main - Entry print
 * Return: Always 0 (Success)
 */
int main(void)

{
	
	char low;

	for (lower = 'a'; low <= 'z'; low++)
	putchar(low);

	for (lower = 'A'; low <= 'Z'; low++)
        putchar(low);
	putchar('\n');

	return (0);
}
