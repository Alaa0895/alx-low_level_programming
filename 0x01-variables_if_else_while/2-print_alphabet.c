#include <stdio.h>
/**
 * main - this is the function of the task
 *
 * Return: always (0) 
 *
 */
int main(void)
{
	char i; = 'a'
	while (i <= 'z')
	{
		putchar(i);
		i++;
	}
	putchar('\n');
        return (0);

}
